// destroy.js.erb

var aiMessageId = "<%= dom_id @ai_message %>";
var aiMessage = document.getElementById(aiMessageId);

// Add burst animation
aiMessage.style.animation = "burst 0.7s ease 0s 1 normal forwards";

// Remove the message element from the DOM after the animation completes
aiMessage.addEventListener("animationend", function() {
  aiMessage.remove();
});
var newAiMessages = document.querySelector('.new_ai_messages');
var groupAiMessages = document.querySelector('.group_ai_messages');

    if (newAiMessages.children.length == 0) {
    console.log("TEST",event.target.classList.contains('delete-button') && newAiMessages.children.length == 0)
       groupAiMessages.remove();
    }
});
