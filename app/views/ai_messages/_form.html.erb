<%= form_with(model: ai_message, local: true) do |form| %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>
  <div class="field">
    <input type="number" name= "job_listing_id" value = "63" >
    <%# <%= form.label :job_listing_id %>
    <%# <%= form.collection_select :job_listing_id, JobListing.all, :id, :title %>
  </div>
  <%= form.submit 'Submit' %>
<% end %>

<turbo-frame id="response_frame">
  <!-- Response from OpenAI will be displayed here -->
</turbo-frame>
